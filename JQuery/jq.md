# 제이쿼리(JQuery)

자바스크립트 언어를 간편하게 사용할 수 있도록 `단순화 시킨 오픈 소스 기반의 자바스크립트 라이브러리`
<br><br>

## 사용 이유

- 제이쿼리는 주요 웹 브라우저의 구버전을 포함한 대부분의 브라우저에서 지원

- 동적으로 `HTML DOM을 손쉽게 조작 가능`, 또한 CSS 스타일도 간단히 적용 가능.
- `짧고 간결한 코딩`으로 불필요한 소스를 줄임.
- 애니메이션 효과나 대화형 처리를 간단하게 적용할 수 있음.
- 오픈 라이센스를 적용하여 누구나 자유롭게 이용 가능.

<br>

## 사용 방법

- 제이쿼리 라이브러리 다운

- 인터넷을 이용하여 파일을 연동시켜 사용(CDN방식)
##### CDN (Content Delivery Network) : 사용자가 요청한 컨텐츠를 사용자와 가장 가까운 곳에 위치한 서버에 전달해 주는 방식
<br>

#### 라이브러리 다운
	<script type="text/javascript" src = "제이쿼리 라이브러리 위치"></script>

#### CDN방식
	<script src = "https://code.jquery.com/jquery-3.5.1.js" ></script>
<br>

## 동작 방법

### 형식
	$("선택자").동작함수("속성", "값");
<br>

### 제이쿼리 실행

	$(function() {
		// 제이쿼리
		$("h2").css("color", "red");
	});
<br>


## 선택자 사용 방법

### 1. 직접 선택자

|종류|방법||
|--|--|--|
|전체 선택자|$("*")|모든 요소(태그)를 선택|
|아이디 선택자|$("**#아이디이름**")|id 속성에 지정된 값을 가진 요소 선택|
|클래스 선택자|$("**.클래스이름**")|class 속성에 지정된 값을 가진 요소 선택|
|요소 선택자|$("**요소(태그)명**")|지정된 요소(태그)명과 일치하는 요소를 선택|
|그룹 선택자|$("**선택1, 선택2, ...선택N**")|선택된 모든 요소를 한번에 선택|
<br>

### 2.인접 관계 선택자

|종류|방법||
|--|--|--|
|부모 요소 선택자|$("요소선택").***parent()***|선택한 요소의 부모 요소 선택|
|상위 요소 선택자|$("요소선택").***parents()***|선택한 요소의 상위 요소들을 선택|
|하위 요소 선택자|$("요소 선택 **하위 요소**")|선택한 요소의 하위 요소를 선택|
|자식 요소 선택자|$("요소 선택 **> 자식요소**")|선택한 요소를 기준으로 자식 관계에 지정한 요소만 선택|
|자식 요소들 선택자|$("요소 선택").***children()***|선택한 요소의 모든 자식 요소를 선택|
|형제(이전) 선택자|$("요소 선택").***prev()***|선택한 요소의 이전 요소를 선택|
|형제(이전) 요소들 선택자|$("요소 선택").***prevAll()***|선택한 요소의 이전 요소들을 선택|
|지정 형제(이전) 요소들 선택자|$("요소 선택").***prevUntil("요소명")***|선택한 요소로부터 지정한 요소 이전까지의 요소들을 선택|
|동생(다음) 요소 선택자|$("요소 선택").***next()***|선택한 요소의 다음 요소를 선택|
|동생(다음) 요소들 선택자|$("요소 선택").***nextAll()***|선택한 요소의 다음요소들을 선택|
<br>

## 제이쿼리 탐색 선택자

직접 선택자를 이용해 선택한 요소 중 `원하는 요소를 한 번 더 탐색`해 선택할 수 있다.

### 종류

#### 1. 위치 탐색자 : 선택한 요소 중 위치를 기준으로 선택

|종류| 방법  |
|--|--|
| 첫번째 요소 선택 |  $("요소선택 ***:first***") or $("요소선택")***.first***|
| 짝수 번째 (홀수 인덱스) 요소 선택 |  $("요소선택 ***:odd***") |
| 홀수 번째 (짝수 인덱스) 요소 선택 |  $("요소선택 ***:even***") |
| 특정 숫자 번째 요소 선택 |  $("요소선택:***nth-child(숫자)***") |
| 특정 배수의 요소 선택 |  $("요소선택:***nth-child(숫자n)***") |
<br>

#### 2. 속성 탐색 선택자 : 요소의 지정된 속성을 기준으로 선택

| 종류 |방법|
|--|--|
| 속성이 있는 요소  | 요소[**속성**] |
| 속성과 값이 `일치`하는 요소 | 요소[**속성 = 값**] |
| 값으로 `시작`하는 요소 | 요소[**속성 ^= 값**] |
| 값으로 시작하는 요소 | 요소[**속성 $= 값**] |
| 값을 `포함`하는 요소 | 요소[**속성 *= 값**] |
| 조건 `2개의 속성`과 값을 `모두 만족`하는 요소 | **요소[속성 = 값][속성 = 값]** |
<br>

#### 3. 탐색 선택자

| 종류  | 방법 |
|--|--|
| 지정한 인덱스가 `참조`하는 요소 | $("요소선택 ***: eq(index)***") |
| 지정한 인덱스보다 `작은` 요소 | $("요소선택 ***: lt(index)***") |
| 지정한 인덱스보다 `큰` 요소 | $("요소선택 : ***gt(index)***") |
<br>

4. 콘텐츠 탐색 선택자 : 요소 내에서 콘텐츠의 포함 여부를 따져서 선택

5. 필터링 선택자 : 선택한 요소를 한번 더 필터링하여 선택

## 요소 조작 메서드

요소를 생성, 복사, 삭제, 속성 변환과 관련된 메서드

### 1. 속성 조작 메서드

| 메서드 |   |
|--|--|
| ***html***() | 선택한 요소에 포함되는 하위 요소를 불러오거나 새 요소로 바꿀 때 사용 |
| ***text***()| 선택한 요소 내에 텍스트를 불러오거나 텍스트를 바꿀 때 사용 |
| ***removeAttr***("속성") | 선택한 요소에서 기존의 속성을 삭제할 때 사용 |
| ***attr***("속성")  / ***attr***("속성 = 값") | 선택한 요소에 새 속성을 추가(생성)하거나, 기존의 속성을 변경할 때 사용 |
| ***addClass***()  |  선택한 요소에 클래스 선택자를 생성할 때 사용 |
| ***removeClass***()| 선택한 요소에 지정된 클래스 선택자를 삭제할 때 사용  |
| ***val***() / ***val***(값)| 입력 요소에 있는 value 값을 불러오거나 변경할 때 사용  |
<br>

### 2. 수치 조작 메서드
### 3. 요소 편집 메서드
복제하거나 새로 생성한 요소를 의도한 위치로 삽입하고 선택한 요소를 삭제하는 기능

| 종류 | 방법 |
|--|--|
| `이전` 위치에 새 요소를 추가 | $("요소선택").***before***("새 요소") |
| `다음` 위치에 새 요소를 추가 | $("요소선택").***after***("새 요소") |
| `마지막` 위치에 새 요소를 추가 | $("요소선택").***append***("새 요소") |
| `마지막` 위치에 새요소를 추가 | $("새 요소").***appendTo***("요소선택") |
| `맨 앞` 위치에 새요소를 추가 | $("요소선택").***prepend***("새 요소") |
| `이전` 위치에 새 요소를 추가 | $("새 요소").***insertBefore***("요소 선택") |
| `다음` 위치에 새 요소를 추가 |  $("새 요소").***insertAfter***("요소 선택") |
| 선택한 요소를 `복사` | $("요소선택").***clone***(true/false) |
| 선택한 요소의 `하위 내용들을 모두 삭제` | $("요소선택").***empty***()|
| 선택한 요소를 `삭제` | $("요소선택").***remove***()|
<br>

## 이벤트 종류

### 1. 마우스 이벤트

| 종류  | 방법  |
|--|--|
| `클릭`했을 때 이벤트를 발생 | $("요소선택").***click***(function(){ ... }) |
| `더블클릭` 했을 때 이벤트를 발생 | $("요소선택").***dbclick***(function(){ ... }) |
| 마우스가 `올라갔을 때` 이벤트 발생 | $("요소선택").***mouseout***(function(){ ... }) |
| 마우스가 `내려갔을 때` 이벤트 발생  | $("요소선택").***mouseover***(function(){ ... }) |
| 마우스가 1 : 올라갔을때 2 : 내려갔을때 | $("요소선택").***hover***(function(){ 1 }, function(){ 2 }) |
| 경계 범위 내에 마우스가 올라갔을 때 이벤트 발생 | $("요소선택").***mouseenter***(function(){ ... }) |
| 경계 범위 내에 마우스가 완전히 떠났을 때 이벤트 발생 | $("요소선택").***mouseleave***(function(){ ... }) |
| 영역에서 마우스를 움직일 때마다 이벤트 발생 | $("요소선택").***mousemove***(function(매개변수) { ... }) |
<br>

### 2. 포커스 이벤트
### 3. 키보드 이벤트 

| 종류 | 방법 |
|--|--|
| 키보드를 눌렀을 때 | $("요소선택").***keypress(function(매개변수){ ... })*** |
| 키보드를 눌렀을 때 | $("요소선택").***keydown(function(매개변수){ ... })*** |
| 키보드에서 손을 떼었을 때 | $("요소선택").***keyup(function(매개변수){ ... })*** |
<br>

## 애니메이션

| 메서드 | 기능 |
|--|--|
| ***hide***("효과 속도(ms)", 콜백함수) | 노출되어 있는 요소를 숨기는 기능 |
| ***show***("효과 속도(ms)", 콜백함수) | 숨겨져 있던 요소를 노출시키는 기능 |
| ***toggle***("효과속도", 콜백함수) | 선택한 요소가 보이면 숨기고, 숨겨져 있으면 보이게 하는 기능 |
| ***fadeIn***("효과속도", 콜백함수) | 숨겨져 있던 요소가 점점 투명해 지면서 노출이 되는 기능 |
| ***fadeOut***("효과속도", 콜백함수) | 노출이 되어 있던 요소가 점점 사라지는 기능 |
| ***fadeToggle***("효과속도", 콜백함수) | 노출이 되어 있던 요소는 점점 사라지고, 사라져 있던 요소는 점점 선명해 지면서 노출이 되는 기능 |
| ***stop***() |  효과 또는 애니메이션이 적용된 효과를 정지시키는 기능 |
| ***delay***(지연시간).애니메이션 효과 메서드() |  효과 또는 애니메이션이 적용된 효과를 지연시키는 기능 |
| ***fadeTo***("효과속도", 투명도(0~1), 콜백함수) | 선택한 요소를 지정한 투명도의 값까지만 숨기는 기능 |
| ***slideUp***("효과속도", 콜백함수) | 요소를 숨기는 기능 |
| ***slideDown***("효과속도", 콜백함수) | 숨겨져 있는 요소를 나타내는 기능 |
| ***slideToggle***("효과속도", 콜백함수) |  요소가 나타나 있으면 숨기고, 숨겨져 있으면 나타내는 기능 |
| ***animate***({애니메이션 속성}, "효과속도", 콜백함수) | 다양한 동작(motion) 효과를 적용하는 기능 |

##### 콜백함수 : 효과가 모두 완료되면 실행되는 함수 (생략 가능)


